{{#each searchResults}}
    <div class="entry">
        <h3>{{{title}}}</h3>
        <div class="body">
            {{{body}}}
        </div>
        <div class="resultUri">
            {{{uri}}}
        </div>
    </div>
{{/each}}